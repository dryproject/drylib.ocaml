(* This is free and unencumbered software released into the public domain. *)

(* Net *)

module Net = DRY__Net

(* Net.IPv4 *)

module IPv4 = DRY__Net.IPv4

(* Net.IPv6 *)

module IPv6 = DRY__Net.IPv6

(* Net.TCP *)

module TCP = DRY__Net.TCP

(* Net.UDP *)

module UDP = DRY__Net.UDP

let () = assert (1 = 1)
