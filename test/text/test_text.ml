(* This is free and unencumbered software released into the public domain. *)

(* Text *)

module Text = DRY__Text

(* Text.ASCII *)

module ASCII = DRY__Text.ASCII

(* Text.UTF8 *)

module UTF8 = DRY__Text.UTF8

let () = assert (1 = 1)
